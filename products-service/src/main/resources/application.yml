server:
  port: 8082
spring:
  application:
    name: products-service
  data:
    mongodb:
      database: watches_db
      port: 27017
      host: localhost
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB